html
    head
        title grid detail
    body
        h3 result
        form(action="/:name/grid_detail/:id" enctype="multipart/form-data" method="POST")
            span Upload Profile Picture:
            input#upload_images(type="file" name="mypic" required="")
            br
            input(type="submit" value="submit")